# Используем официальный PHP-образ
FROM php:7.4-cli

# Устанавливаем рабочую директорию
WORKDIR /app

# Копируем все файлы проекта
COPY . .

# Указываем команду для запуска приложения
CMD ["php", "-S", "0.0.0.0:32777"]

